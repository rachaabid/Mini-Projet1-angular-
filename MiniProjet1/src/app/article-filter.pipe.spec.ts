import { ArticleFilter.Pipe } from './article-filter.pipe';

describe('ArticleFilter.Pipe', () => {
  it('should create an instance', () => {
    expect(new ArticleFilter.Pipe()).toBeTruthy();
  });
});
